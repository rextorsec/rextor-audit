[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"

# SPEC-4 §2 — deployment target #1 (Tempo testnet / Moderato, flagship track).
# No native gas token: fees for plain EVM txs to non-TIP-20 contracts are paid
# in pathUSD held by the broadcaster (faucet: tempo_fundAddress RPC).
[profile.tempo]
eth_rpc_url = "https://rpc.moderato.tempo.xyz"
chain_id = 42431

# SPEC-4 §2 — deployment target #2 (HyperEVM testnet, primary track). v2 deploys
# DIRECT here (no v1 history). Native gas token: HYPE (NOT pathUSD — that is
# Tempo-only). Verified 2026-09-18: the bare domain 404s; only the `/evm` path
# serves JSON-RPC (eth_chainId → 0x3e6). Docs: hyperliquid.gitbook.io,
# for-developers/hyperevm (chainId 998, Cancun, EIP-1559, 18-decimal HYPE).
[profile.hyperliquid]
eth_rpc_url = "https://rpc.hyperliquid-testnet.xyz/evm"
chain_id = 998

# SPEC-4 §2 — deployment target #2 MAINNET (2026-09-21): faucet-gated testnet
# abandoned in favor of mainnet receipts (gas 0.1 gwei → Gate B + months of
# attestations ≈ 0.002 HYPE). Same HYPE-native gas token, chainId 999.
[profile.hyperliquid-mainnet]
eth_rpc_url = "https://rpc.hyperliquid.xyz/evm"
chain_id = 999

# C1 — ERC-8004 canonical identity registries (docs/deployments/erc8004.md).
# Same canonical address on both chains (deterministic CREATE2); code probed
# live 2026-09-19. eth_rpc_url here is a health fallback — override with
# --rpc-url if the pinned public RPC degrades.
[profile.eth]
eth_rpc_url = "https://ethereum-rpc.publicnode.com"
chain_id = 1

[profile.base]
eth_rpc_url = "https://mainnet.base.org"
chain_id = 8453
