# Provenance

- `lib/forge-std`: vendored from upstream `foundry-rs/forge-std` master @ `7fdf81f9ceb2f6ebbb8f9f1c6c5274d5bcc9a1f5` (2026-09-10) via `forge install --no-git`. The `package.json` inside it still labels itself 1.16.2 — that tag lags upstream; the vendored tree contains 5 post-tag files, so byte-identity is against the commit, not the release tag.
