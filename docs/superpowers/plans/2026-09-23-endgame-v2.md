# END GAME v2 — Submission Runway (post-table tier)

**Date:** 2026-09-23 · **Provenance:** session with RECTOR ("whats our endgame menu?" — endgame
framing kept, versioned). **Base:** main @ `7b6427a`, PRs through #33 merged, zero open
PRs/issues/TODOs. **Horizon:** Sep 23 → CWF submission (opens **Oct 6**, internal **Oct 10**,
hard **Oct 12**).

## 1. Naming — endgame ladder

| Version | Exit state | Status |
|---|---|---|
| **END GAME v1** | Demo-complete: end-game table 15/15 shipped with live receipts (`content/capabilities.json`) | ✅ **CLOSED** 2026-09-22 (HyperEVM mainnet twin, config-gate check-run receipt) |
| **END GAME v2** | Submission-complete: CWF submission filed | 🔓 open — this doc |

The marketing artifact keeps its name ("end-game table"). The phase ladder is versioned.
Deferrals reference stable IDs from §3, never re-litigate scope.

## 2. Freeze note

Roadmap freeze checkpoint is **Sep 24** (tomorrow): after freeze, receipts/polish/video only.
R1/R2 are scope additions — they must land **before** freeze or get an explicit RECTOR
freeze extension. Everything else in §3 is freeze-compatible by nature.

## 3. END GAME v2 menu

Costs are focused agent-days. ~17d available; full menu ≈ 7d + RECTOR stack → no forced cuts.

### Track A — Coding (CIPHER)

| ID | Item | Cost | Value | Depends / note |
|---|---|---|---|---|
| R1 | Rider-chain coverage: Eth L1 / Base / Arbitrum / Robinhood as `targetChainId` metadata in pipeline + web surface (no native deploys — home-chain anchor, locked architecture) | 0.5–1d | H — CWF aimed tracks include EVM riders; registry holds only Tempo 42431 + HyperEVM 999 today | `packages/web/lib/chains.ts`, SPEC-5; **pre-freeze** |
| R2 | C2 live ingestion: auto `giveFeedback` on every completed review; identity-card disclosure upgrades "operator-seeded" → "automated" | ~1d | M–H — flips a disclosed roadmap item to shipped | feedback scripts exist as manual ops; **pre-freeze** |
| R3 | Graceful drain (F7 remnant): SIGTERM → finish in-flight review, stop accepting new | 0.5d | L–M — #32 added watchdog; shutdown still hard-cuts | `packages/agent/src/server.ts` |
| R4 | F5 code-half: comment-posting swap to App installation tokens behind env flag (code + mock tests now; activates on PEM) | 0.5d | M — kills the PAT dependency | `github.ts`, PR #24 pattern; needs App PEM to go live |
| R5 | `/compare` route — named CodeRabbit table, SEO play | 0.5d | M | RECTOR pick; **default skip** |
| R6 | E2 comparison harness: glm-5.3 vs baseline verdict-quality receipt on a FRESH trigger PR | 0.5d | M — formal comparison never captured; demo appendix material | E2 rule: fresh PR only |

### Track B — Submission (mixed owner)

| ID | Item | Owner | Cost | Note |
|---|---|---|---|---|
| V1 | Demo-week re-verification: `capabilities.json` `asOf` (stale @ 2026-09-19), competitor snapshot refresh (CodeRabbit ships weekly), receipts-still-live probe, rot-grep | CIPHER | 0.5d | Matrix rule 5 — **required before Oct 10** |
| J1 | Judges 3-way matrix → submission docs (CodeRabbit / Conatus / Rextor) | CIPHER | 0.5d | `competitive-positioning.md` is ~80% source; judge-facing, never public landing |
| E3 | Demo video: script red-pen → record → edit (money shot: stale audit PDF vs live catch) | RECTOR + CIPHER | 1–1.5d | `docs/demo/script.md` drafted; Conatus polish bar ($17.5k); **highest-EV item** |

### Track C — RECTOR manual stack

| Item | Note |
|---|---|
| ~~Vercel: clear Root Directory + SSO toggle off~~ DONE 2026-09-24 | CIPHER via API PATCH (`rootDirectory`+`ssoProtection` → null on `rextor-audit-web`); plain `vercel --prod` from packages/web verified, alias moved, `*.vercel.app` now public |
| Sweep 7 stale classic PATs | VPS-era, no expiry; `.env` token (exp 2027) is load-bearing — keep |
| rextorsecurity.com DNS | No DNS as of 2026-09-22; never link until set |
| CWF form finish | Opens Oct 6; project registered, Team 1/1 complete |
| F2: stale `OPENROUTER_API_KEY` shell export purge | 401-poisons interactive shells without `env -u` wrapper |

## 4. Drop-order (RECTOR owns picks)

R5 → R6 → R4 (PAT valid to 2027 — no urgency). **Protected spine: V1 + J1 + E3** (the
submission itself) + R1 recommended-first (only item widening submission breadth against
aimed tracks).

## 5. Exit condition

END GAME v2 closes when the CWF submission is filed (internal target **Oct 10**, hard
**Oct 12**). Each landed item flips its surface (capabilities.json / docs) same day —
receipts-not-claims, invariant 17.
