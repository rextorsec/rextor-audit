# Week 3 — Scope & Decision Record (persisted pre-kickoff)

**Date:** 2026-09-18 · **Provenance:** roadmap session with RECTOR (post T10-complete,
main @ `8e2649f`). RECTOR locked the full scope ("why not all of them, powerful, we have a
time right?") and owns deferral picks. **Status:** pre-kickoff artifact — the Week-3
kickoff (SPEC-6 → mockups → Next.js) absorbs this verbatim; IDs are stable for deferral
syntax (`defer B6, D5, …`).

Companion artifacts: `docs/superpowers/plans/2026-09-18-week-3-marketing-matrix.md`
(end-game table + row registry) · SPEC-4 errata block (v2 decision pointer).

---

## 1. Decision log (locked 2026-09-18)

1. **Verdict-per-chain architecture = home-chain + native demo chains.** Contract v2 adds
   `targetChainId` (chain the audited code targets) + `findingsURI`. Native deploys for
   Tempo (live), HyperEVM, Solana devnet (iff the Solana pair ships); riders
   (Ethereum/Base/Arbitrum/Robinhood) NEVER get native deploys — home-chain anchor +
   `targetChainId` covers them (SPEC-5 config-level rule stands).
2. **Contract v2 (B1):** immutable-by-design → redeploy on Tempo; testnet `reviewCount`
   reset is acceptable and documented. `findingsURI` = IPFS-pinned full report (crib
   `conatus/agent/src/ipfs.ts` + `anchor.ts`; Pinata JWT likely reusable).
3. **Solana pair inseparability:** Anchor verdict program + semgrep-over-Anchor review
   slice on `fixtures/solana-vault` ship together or not at all — a verdict program with no
   review path behind it is attestation theater (judges poke it).
4. **ERC-8004 (C1/C2):** identity chain-neutral (Tempo registry if present, else
   Ethereum-mainnet canonical registries); verdicts chain-native. Reputation ingestion
   (giveFeedback) operator-seeded à la Conatus with honest disclosure.
5. **CodeRabbit adoption scope:** per-finding fix diff blocks (suggested-only, NEVER
   auto-PR), `rextor.yaml` repo config with severity→check-run gate, evidence-forward
   comments, AND dismissals/learnings memory — dismissals store MUST be server-side keyed
   by repo (SQLite in service container); a repo-file store is an injection vector
   (untrusted PR content could silence findings).
6. **Marketing matrix:** end-game table persisted in the companion doc; `soon` badge
   budget max 2 rows; receipts-not-claims rule; dated-snapshot footnote.
7. **Freeze checkpoints:** Sep 24 = scope-freeze + PAT renewal + CWF registration;
   demo-week re-verification of the competitor snapshot.

## 2. Full scope menu

Costs are focused agent-days. ~15d available before Oct 10; full-IN ≈ 18–24d → deferral
picks close the gap.

| ID | Item | Cost | Demo value | Risk / note | Depends on |
|---|---|---|---|---|---|
| A1 | SPEC-6 authoring (product surface) | 0.5d | — | spec at phase start, first | — |
| A2 | Hallmark mockups → RECTOR approval | 1d | H | hard rule; approval is the gate | A1 |
| A3 | Next.js: landing + dashboard + install flow | 2–3d | H | Vercel; the submission | A2 ✅ |
| A4 | Dashboard views: attestation ledger, score history, `verify()` links, identity card | (in A3) | H | don't copy CR deps/secrets/SBOM | A3 |
| B1 | Contract v2 (`findingsURI` + `targetChainId`) + Tempo redeploy | 1–1.5d | H | reviewCount reset documented | — |
| B2 | HyperEVM deploy #2 (deploys v2 directly — saves a redeploy) | 0.5d | M | pattern reuse; faucet | B1 |
| B3 | IPFS pin + attestation footer v2 | 0.5–1d | H | explorer → IPFS → recompute = 3-step judge demo | B1 |
| B4+B5 | Solana pair: Anchor verdict program (devnet) + semgrep-over-Anchor fixture slice | 2–3d | H | inseparable; biggest unknown (new toolchain, funding) | — (parallel) |
| B6 | Mainnet attestation garnish (Mantle or Base) | 0.5d | M | story only; Conatus carries "real gas" | B1 |
| C1 | ERC-8004 registration (Tempo if present, else ETH mainnet) + cite agentId | 0.5–1d | M–H | 15-min registry-availability spike first | B1 |
| C2 | Reputation ingestion (ERC-8004 giveFeedback, operator-seeded) | 1d | M | honest-disclosure pattern proven in Conatus | C1 |
| D1 | Per-finding fix diff blocks (suggested-only, never auto-PR) | 1d | H | prompt + renderer | — |
| D2 | `rextor.yaml` (paths, ignore globs, severity gate → check-run conclusion) | 1d | H | the merge-gate story | — |
| D3 | Evidence-forward comment (quoted cited lines + verdict banner) | 0.5d | M | presentation polish | — |
| D4 | Dismissals/learnings memory — server-side SQLite keyed by repo | 1.5–2d | M | repo-file store forbidden (injection) | — |
| D5 | `@rextor-audit` reply/chat in PR | 1–1.5d | M | new `issue_comment` path; comment content = untrusted injection surface | — |
| E1 | Chain cost-model tool (`tempo_fee_review`-class) | 1–1.5d | M | Conatus `mantle_gas_review` parity | B2 |
| E2 | Model flip rehearsal (`glm-5.3`) + fresh trigger PR | 0.5d | M | waits on RECTOR un-defer; new branch/PR rule | — |
| E3 | Demo video (3-min) + script | 1–1.5d | H | Conatus won $17.5k on polish | most above |
| E4 | `docs/competitive-positioning.md` | 0.5d | M | fills Gate A's declared CodeRabbit gap; ~80% exists in the 2026-09-18 session | — |
| F1 | PAT renewal | RECTOR | — | **hard: expires Sep 24** | — |
| F2 | Stale `OPENROUTER_API_KEY` shell export purge | RECTOR | — | 401s without `env -u` wrapper | — |
| F3 | CWF registration + multi-track check | RECTOR | H | submission prerequisite | — |
| F4 | Domains: rextoraudit.com + rextorsecurity.com | RECTOR | — | landing live in Week 5 | — |
| F5 | PEM + swap comment-posting to installation tokens | 0.5d + RECTOR PEM | M | kills the PAT dependency | PEM (manual) |
| F6 | Tunnel webhook URL update on restart | 5 min | — | standing op (ephemeral quick tunnel) | — |
| F7 | Receipt-status hardening + graceful drain (Week-2 minors) | 0.5d | L | robustness polish | — |

## 3. Drop-order (informational — RECTOR owns final picks)

1. B6 mainnet garnish → 2. D4 dismissals memory → 3. B4+B5 Solana pair (as a unit).
Protected spine: B1 + B3 + C1 + B2 (proof asymmetry + ledger core survives all cuts).

## 4. Cross-references

- Marketing end-game table + row registry: `docs/superpowers/plans/2026-09-18-week-3-marketing-matrix.md`
- SPEC-4 errata block (2026-09-18): v2 decision pointer; §1/§2 describe v1, superseded at v2 authoring
- Handoff: `~/.local/handoffs/rextor-audit/session-handoff-2026-09-17-b.md` (T10 state, infra, gotchas)
- Gate docs: `docs/gates/gate-a-competitive-scan-2026-09-15.md` (CodeRabbit §6 declared gap — filled by E4), `docs/gates/gate-b-aderyn-2026-09-15.md` (Solana pivot rationale — Solana pair rides Gate B′ framing)

## 5. Acceptance (this artifact)

- Every Week-3/4 work item is on the menu with an ID; deferrals reference IDs, never re-litigate scope.
- SPEC-6 at kickoff cites this doc as its scope source; the marketing matrix doc is its content source.
